# Copy this file to .env locally and fill in your real keys.
# Do not commit the real .env file.

GEMMA_API_KEY=
# Set to true only when you want live Gemini/Gemma calls; the local rule-based
# explanation fallback keeps the complete workflow runnable without a key.
GEMMA_ENABLED=false
MODEL_NAME=gemma-3n-e4b-it
OPENAI_API_KEY=
HF_TOKEN=

BACKEND_PORT=8000
FRONTEND_PORT=3000
APP_ENV=development
